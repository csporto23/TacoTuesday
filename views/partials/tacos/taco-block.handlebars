<li>
	{{name}}

	<button class="change-pickedUp" data-id="{{id}}" data-newpickedUp="{{pickedUp}}">
		{{#if pickedUp}}That one has been taken{{else}}pick up your taco{{/if}}
	</button>

	<button class="delete-taco" data-id="{{this.id}}">remove order</button>
</li>
