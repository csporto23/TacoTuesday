<h1>Taco Time</h1>

<h2>Welcome to the TacoTuesday truck</h2>

<ul>
  {{#each tacos}}
    {{#unless pickedUp}}
      {{> tacos/taco-block pickedUp=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Picked up tacos!</h2>

<ul>
  {{#each tacos}}
    {{#if pickedUp}}
      {{> tacos/taco-block pickedUp=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Taco</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ta">Taco Name:</label>
    <input type="text" id="ta" name="taco_name">
  </div>

  <div class="form-group">
    <label for="vegetarian">Vegetarian?</label><br>
    <input type="radio" name="vegetarian" value="1" checked> Vegetarian!<br>
    <input type="radio" id="vegetarian" name="vegetarian" value="0"> Not Vegetarian!<br>
  </div>

  <div class="form-group" id="shell">
    <label for="vegetarian">Corn or flour?</label><br>
    <input type="radio" name="shell" value="hard/corn" checked> Corn<br>
    <input type="radio" name="shell" id="floor" value="soft/flour"> Flour
  </div>

  <button type="submit">Add your Taco</button>
</form>